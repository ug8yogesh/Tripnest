name: Backend CI

on:
  push:
    paths:
      - 'tripnest-backend/**'
      - '.github/workflows/backend-ci.yml'
  pull_request:
    paths:
      - 'tripnest-backend/**'
      - '.github/workflows/backend-ci.yml'

jobs:
  test:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: tripnest-backend

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Java 25
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '25'
          cache: maven

      - name: Run tests
        run: chmod +x mvnw && ./mvnw -B test